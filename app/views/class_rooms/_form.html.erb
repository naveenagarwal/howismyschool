<%= form_for(@class_room) do |f| %>
  <% if @class_room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@class_room.errors.count, "error") %> prohibited this class room from being saved:</h2>

      <ul>
      <% @class_room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :grade %>
    <%= f.text_field :grade %>
  </p>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
