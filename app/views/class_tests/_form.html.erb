<%= form_for(@class_test) do |f| %>
  <% if @class_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@class_test.errors.count, "error") %> prohibited this class_test from being saved:</h2>

      <ul>
      <% @class_test.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
